<%= form_for(@walk) do |f| %>
  <% if @walk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@walk.errors.count, "error") %> prohibited this walk from being saved:</h2>

      <ul>
      <% @walk.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :song_link %><br />
    <%= f.text_field :song_link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
